Suppose T\_{v}^{1,0}({D\_{\mathrm{reg}}^{\*}})\oplus\mathbb{C}\{v\}=\mathbb{C}^{n}, then the intersection number of \mathbb{C}\{v\} and D\_{\mathrm{reg}}^{\*} at v is 1, i.e. i\_{v}(\mathbb{C}\{v\},D\_{\mathrm{reg}}^{\*})=1.
Indeed, let v\in D\_{\mathrm{reg}}^{\*}\subset\mathbb{C}^{n}\setminus\{0\},
then there exists h\in\mathcal{O}\_{\mathbb{C}^{n},v} such that (Z(h))\_{v}=(D\_{\mathrm{reg}}^{\*})\_{v} and \nabla h(v)\neq 0.
The intersection number is given by i\_{v}(\mathbb{C}\{v\},D\_{\mathrm{reg}}^{\*})=\operatorname{ord}\_{1}h\_{v}(z), where h\_{v}(z):=h(zv). We have {h^{\prime}\_{v}}(1)=v\cdot\nabla h(v), and assume {h^{\prime}\_{v}}(1)=0, by
T\_{v}^{1,0}({D\_{\mathrm{reg}}^{\*}})=\{x\in\mathbb{C}^{n}\mid x\cdot\nabla h(v)%
=0\}. We have v\in T\_{v}^{1,0}({D\_{\mathrm{reg}}^{\*}}), which implies \mathbb{C}\{v\}\subseteq T\_{v}^{1,0}({D\_{\mathrm{reg}}^{\*}}).
But this contradicts T\_{v}^{1,0}({D\_{\mathrm{reg}}^{\*}})\oplus\mathbb{C}\{v\}=\mathbb{C}^{n}. Therefore, h^{\prime}\_{v}(1)\neq 0, hence
\operatorname{ord}\_{1}h\_{v}(z)=1, and consequently i\_{v}(\mathbb{C}\{v\},D\_{\mathrm{reg}}^{\*})=1.

By the above lemma, we have the following proposition.

###### Proposition 2.5.

With the above notations, we have

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\int\_{S\_{p}(1)}m\_{f\_{\vec{a}}}(r,s,D)\,\sigma\_{p}(\vec{a}) | \displaystyle=m\_{f}(r,s,D), |  |
|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\int\_{S\_{p}(1)}T\_{f\_{\vec{a}}}(r,s,D)\,\sigma\_{p}(\vec{a}) | \displaystyle=T\_{f}(r,s,D)+O(1), |  |
|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\int\_{S\_{p}(1)}N\_{f\_{\vec{a}}}^{(k)}(r,D)\ \sigma\_{p}(\vec{a}) | \displaystyle=N\_{f}^{(k)}(r,D)+O(\log r),\ \text{for any }1\leq k\leq\infty. |  |

###### Proof.

The first equality holds by Lemma 2.3 and the definition of proximity function. For the second equality, without loss of generality, we assume that D is very ample, since any Cartier divisor can be written as the difference of two ample divisors. Then D induces a canonical embedding \iota:X\hookrightarrow\mathbb{P}^{N}
for some N, such that
\iota^{\*}\mathcal{O}\_{\mathbb{P}^{N}}(1)\cong[D].
Denote by \omega\_{FS} the Fubini-Study metric on \mathbb{P}^{N}. Then:

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\int\_{s}^{r}\frac{dt}{t^{2p-1}}\int\_{B\_{p}(t)}f^{\*}\iota^{\*}% \omega\_{FS}\wedge v\_{p}^{p-1} | \displaystyle=\int\_{s}^{r}\frac{dt}{t^{2p-1}}\int\_{B\_{p}(t)}f^{\*}c\_{1}[D]% \wedge v\_{p}^{p-1} |  |
|  |  |  |  |
| --- | --- | --- | --- |
|  |  | \displaystyle=T\_{f}(r,s,D)+O(1). |  |

By the Green-Jensen formula (see [19], [20]), we have