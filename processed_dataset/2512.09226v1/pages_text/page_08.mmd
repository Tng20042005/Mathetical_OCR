|  |  |  |
| --- | --- | --- |
|  | =\min\_{l\in\mathbb{Z}/n}\left(\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l% ,\tau(k))+diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1}(p)\neq\tau^{-1}(p-l)\})\right) |  |

## 3. Splitting the minimum into two terms

{lemm}

For any \pi,\tau\in Sym\_{n}, we have

|  |  |  |
| --- | --- | --- |
|  | \min\_{l\in\mathbb{Z}/n}\left(\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l,% \tau(k))+diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1}(p)\neq\tau^{-1}(p-l)\})\right) |  |

|  |  |  |
| --- | --- | --- |
|  | \leq 2\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l,% \tau(k))+\min\_{l\in\mathbb{Z}/n}diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1}(p)% \neq\tau^{-1}(p-l)\}) |  |

###### Proof.

Note that the direction \geq is trivial (when we drop the constants 4 and 3).
The point of this claim is to show the reverse non-trivial direction.
We will write:

|  |  |  |
| --- | --- | --- |
|  | T\_{1}:=\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l% ,\tau(k)),\;\;\;T\_{2}:=\min\_{l\in\mathbb{Z}/n}diam\_{\mathbb{Z}/n}(\{0,l\}\cup% \{p:\pi^{-1}(p)\neq\tau^{-1}(p-l)\}) |  |

  

CASE A: T\_{1}=\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l,%
\tau(k))\geq n/2
  
Observe that always T\_{2}\leq n, so the second term is of lower order compared to the first term, and we get the trivial estimate:

|  |  |  |
| --- | --- | --- |
|  | \min\_{l\in\mathbb{Z}/n}\left(\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l,% \tau(k))+diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1}(p)\neq\tau^{-1}(p-l)\})\right) |  |

|  |  |  |
| --- | --- | --- |
|  | \leq T\_{1}+n/2\leq T\_{1}+T\_{1}\leq 2T\_{1}+T\_{2}. |  |

  

CASE B: T\_{1}=\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l,%
\tau(k))<n/2
  
Note that in this case |\{k:\pi(k)-l\neq\tau(k)\}|<n/2 for any value of l which attains this minimum.
Also, via a change of variables:

|  |  |  |
| --- | --- | --- |
|  | |\{k:\pi(k)-l\neq\tau(k)\}|=|\{k:k-l\neq\tau(\pi^{-1}(k))\}|=|\{k:\tau^{-1}(k-% l)\neq\pi^{-1}(k)\}|, |  |

so for any other value l^{\prime}\neq l we get that

|  |  |  |
| --- | --- | --- |
|  | diam\_{\mathbb{Z}/n}(\{0,l^{\prime}\}\cup\{p:\pi^{-1}(p)\neq\tau^{-1}(p-l^{% \prime})\})\geq|\{k:\tau^{-1}(k-l^{\prime})\neq\pi^{-1}(k)\}| |  |

|  |  |  |
| --- | --- | --- |
|  | \geq|\{k:\tau^{-1}(k-l)=\pi^{-1}(k)\}|>n/2. |  |