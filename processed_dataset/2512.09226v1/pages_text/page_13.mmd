|  |  |  |
| --- | --- | --- |
|  | =\dfrac{1}{8}\min\_{l\in\mathbb{Z}/n}diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1% }(p)\neq\tau^{-1}(p-l)\}), |  |

and the proof of Lemma 5 is complete.

## 6. Putting everything together and finishing the proof of theorem 1:

Let \Phi\_{1} and \Phi\_{2} be the two embeddings in the above two sections
and take their direct sum \Phi=\Phi\_{1}\oplus\Phi\_{2}:\pi\mapsto(\Phi\_{1}(\pi),\Phi\_{2}(\pi))\in L\_{1}%
\oplus L\_{1}. We may rescale the map \Phi\_{1} so that ||\Phi\_{1}||\_{Lip}\leq 1 and ||\Phi\_{1}^{-1}||\_{Lip}\leq 2\pi (where by \Phi\_{1}^{-1} we mean the inverse on the image).
  
For all \pi,\tau\in Sym\_{n} we have two cases:
  
CASE 1: \min\_{l}\sum\_{k}d(\pi(k),\tau(k)+l)\geq n/3.
For the lower bound:

|  |  |  |
| --- | --- | --- |
|  | d(\pi,\tau)\geq\dfrac{1}{3}\min\_{l\in\mathbb{Z}/n}\left(\sum\_{k\in\mathbb{Z}/n% }d\_{\mathbb{Z}/n}(\pi(k)-l,\tau(k))+diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1% }(p)\neq\tau^{-1}(p-l)\})\right) |  |

|  |  |  |
| --- | --- | --- |
|  | \geq\dfrac{1}{3}\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}% (\pi(k)-l,\tau(k))\geq\dfrac{1}{3}||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1} |  |

|  |  |  |
| --- | --- | --- |
|  | \geq\dfrac{1}{3}||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}+\dfrac{1}{3}||\Phi\_{2}(% \pi)-\Phi\_{2}(\tau)||\_{1}-\dfrac{4}{3}\;d(\pi,\tau). |  |

where we used ||\Phi\_{2}(\pi)-\Phi\_{2}(\tau)||\_{1}\leq 4d(\pi,\tau).
After rearranging terms, we obtain:

|  |  |  |
| --- | --- | --- |
|  | d(\pi,\tau)\geq 7(||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}+||\Phi\_{2}(\pi)-\Phi\_{2% }(\tau)||\_{1}). |  |

while for the upper bound:

|  |  |  |
| --- | --- | --- |
|  | d(\pi,\tau)\leq 6\min\_{l\in\mathbb{Z}/n}\left(\sum\_{k\in\mathbb{Z}/n}d\_{% \mathbb{Z}/n}(\pi(k)-l,\tau(k))+diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1}(p)% \neq\tau^{-1}(p-l)\})\right) |  |

|  |  |  |
| --- | --- | --- |
|  | \leq 12\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l% ,\tau(k))+6\min\_{l\in\mathbb{Z}/n}diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{p:\pi^{-1}(% p)\neq\tau^{-1}(p-l)\}) |  |

|  |  |  |
| --- | --- | --- |
|  | \leq 12\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l% ,\tau(k))+3n\leq(12+9)\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb% {Z}/n}(\pi(k)-l,\tau(k)) |  |

|  |  |  |
| --- | --- | --- |
|  | \leq 21\times 2\pi||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}\;\leq\;21\times 2\pi(||% \Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}+||\Phi\_{2}(\pi)-\Phi\_{2}(\tau)||\_{1}) |  |

  

CASE 2: \min\_{l}\sum\_{k}d(\pi(k),\tau(k)+l)<n/3.
For the lower bound

|  |  |  |
| --- | --- | --- |
|  | ||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}+||\Phi\_{2}(\pi)-\Phi\_{2}(\tau)||\_{1}\leq% \min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/n}(\pi(k)-l,\tau(k% ))+4d(\pi,\tau)\leq 7d(\pi,\tau), |  |

while for the upper bound:

|  |  |  |
| --- | --- | --- |
|  | d(\pi,\tau)\leq 12\min\_{l\in\mathbb{Z}/n}\sum\_{k\in\mathbb{Z}/n}d\_{\mathbb{Z}/% n}(\pi(k)-l,\tau(k))+6\min\_{l\in\mathbb{Z}/n}diam\_{\mathbb{Z}/n}(\{0,l\}\cup\{% p:\pi^{-1}(p)\neq\tau^{-1}(p-l)\}) |  |

|  |  |  |
| --- | --- | --- |
|  | \leq 12\times 2\pi||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}+6\times 8||\Phi\_{2}(\pi% )-\Phi\_{2}(\tau)||\_{1}\leq 12\times 2\pi(||\Phi\_{1}(\pi)-\Phi\_{1}(\tau)||\_{1}+% ||\Phi\_{2}(\pi)-\Phi\_{2}(\tau)||\_{1}) |  |

Combining all the constants from the upper and lower bounds:

|  |  |  |
| --- | --- | --- |
|  | c\_{1}(\Gamma(Sym\_{n},\{t,c\}))\leq 7\times 21\times 2\pi\approx 923.63<1000. |  |