|  | \|f\_{x}\|^{2}\_{L^{2}}+\|g\|^{2}\_{L^{2}}\lesssim\int\_{G}|u\_{t}|^{2}{\,\rm d}x{% \,\rm d}t\quad\mbox{ for all }(f,g)\in\dot{H}^{1}({\mathbb{T}})\times L^{2}({% \mathbb{T}}). |  |

Using d’Alembert’s formula, this is equivalent to

|  |  |  |
| --- | --- | --- |
|  | \|f\_{x}\|^{2}\_{L^{2}}+\|g\|^{2}\_{L^{2}}\lesssim\int\_{G}|f^{\prime}(x+t)-f^{% \prime}(x-t)+g(x+t)+g(x-t)|^{2}{\,\rm d}x{\,\rm d}t\quad\mbox{ for all }(f,g)% \in\dot{H}^{1}({\mathbb{T}})\times L^{2}({\mathbb{T}}). |  |

Replacing f^{\prime} by f, we have

|  |  |  |
| --- | --- | --- |
|  | \|f\|^{2}\_{L^{2}}+\|g\|^{2}\_{L^{2}}\lesssim\int\_{G}|f(x+t)-f(x-t)+g(x+t)+g(x-t% )|^{2}{\,\rm d}x{\,\rm d}t\quad\mbox{ for all }(f,g)\in L^{2}({\mathbb{T}})% \times L^{2}({\mathbb{T}}). |  |

Letting f=g and f=-g, we obtain

|  |  |  |
| --- | --- | --- |
|  | \|g\|^{2}\_{L^{2}}\lesssim\int\_{G}|g(x+t)|^{2}{\,\rm d}x{\,\rm d}t\quad\mbox{ % for all }g\in L^{2}({\mathbb{T}}) |  |

and

|  |  |  |
| --- | --- | --- |
|  | \|g\|^{2}\_{L^{2}}\lesssim\int\_{G}|g(x-t)|^{2}{\,\rm d}x{\,\rm d}t\quad\mbox{ % for all }g\in L^{2}({\mathbb{T}}) |  |

respectively. According to the necessary condition of observability of transport equation, see Theorem A.5, we find that G is necessary to satisfy (GCC).
∎

### A.4. A basic integration lemma

###### Lemma A.7.

Let T>0. Let A,B\subset\mathbb{R} be measurable sets.
Assume that u\in C^{1}(\mathbb{R}^{2}) (or at least u is continuously differentiable on an open set containing \Omega). Then

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\iint\_{\Omega^{+}(B;T)}\bigl{(}u\_{x}+u\_{t}\bigr{)}{\,\rm d}x{\,% \rm d}t | \displaystyle=\int\_{T+B}u(x,T){\,\rm d}x-\int\_{B}u(x,0){\,\rm d}x, |  |
|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\iint\_{\Omega^{-}(A;T)}\bigl{(}u\_{x}-u\_{t}\bigr{)}{\,\rm d}x{\,% \rm d}t | \displaystyle=\int\_{A}u(x,0){\,\rm d}x-\int\_{A-T}u(x,T){\,\rm d}x. |  |

###### Proof.

We only prove the first equation, since the second one can be proved similarly. Perform the change of variables

|  |  |  |
| --- | --- | --- |
|  | \eta=x-t,\qquad\tau=t, |  |

with inverse transformation x=\eta+\tau, t=\tau. The Jacobian determinant equals 1.
This map transforms \Omega into

|  |  |  |
| --- | --- | --- |
|  | \Phi(\Omega^{+})=\{(\eta,\tau)\mid 0\leq\tau\leq T,\;\eta\in B\}=B\times[0,T]. |  |

Define v(\eta,\tau)=u(\eta+\tau,\tau). Then

|  |  |  |
| --- | --- | --- |
|  | \frac{\partial v}{\partial\tau}=u\_{x}(\eta+\tau,\tau)\cdot 1+u\_{t}(\eta+\tau,% \tau)=u\_{x}+u\_{t}. |  |

By the change-of-variables formula,

|  |  |  |
| --- | --- | --- |
|  | \iint\_{\Omega^{+}}(u\_{x}+u\_{t}){\,\rm d}x{\,\rm d}t=\iint\_{B\times[0,T]}\frac{% \partial v}{\partial\tau}{\,\rm d}\eta{\,\rm d}\tau. |  |

Using Fubini’s theorem on the product region,