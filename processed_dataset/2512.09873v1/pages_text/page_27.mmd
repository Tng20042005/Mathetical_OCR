|  |  |  |
| --- | --- | --- |
|  | \displaystyle\int\_{G}|\sum\_{k\in\mathbb{Z}}A\_{k}\mathrm{e}^{\mathrm{i}k(t+x)}|% ^{2}{\,\rm d}x{\,\rm d}t=\int\_{{\mathbb{T}}^{2}}|\sum\_{k\in\mathbb{Z}}A\_{k}% \mathrm{e}^{\mathrm{i}k(t+x)}|^{2}\mathbf{1}\_{G}(t,x){\,\rm d}x{\,\rm d}t=\int% \_{{\mathbb{T}}}|\sum\_{k\in\mathbb{Z}}A\_{k}\mathrm{e}^{\mathrm{i}ky}|^{2}\int\_{% {\mathbb{T}}}\mathbf{1}\_{G}(t,y-t){\,\rm d}t{\,\rm d}y. |  |

Using (GCC), we know that for a.e. y\in{\mathbb{T}}, \int\_{{\mathbb{T}}}\mathbf{1}\_{G}(t,y-t){\,\rm d}t\geq c\_{0}>0. Therefore, we derive that

|  |  |  |
| --- | --- | --- |
|  | |\sum\_{k\in\mathbb{Z}}A\_{k}\mathrm{e}^{\mathrm{i}ky}|^{2}\int\_{{\mathbb{T}}}% \mathbf{1}\_{G}(t,y-t){\,\rm d}t\geq c\_{0}|\sum\_{k\in\mathbb{Z}}A\_{k}\mathrm{e}% ^{\mathrm{i}ky}|^{2},\;\mbox{ for a.e. }y\in{\mathbb{T}}. |  |

By Plancherel’s theorem, \int\_{{\mathbb{T}}}|\sum\_{k\in\mathbb{Z}}A\_{k}\mathrm{e}^{\mathrm{i}ky}|^{2}{%
\,\rm d}y=2\pi\sum\_{k\in\mathbb{Z}}|A\_{k}|^{2}=2\pi\sum\_{k\in\Lambda}|a\_{k}|^{2}. As a consequence, we obtain the observability (A.9):

|  |  |  |
| --- | --- | --- |
|  | 2\pi c\_{0}\sum\_{k\in\Lambda}|a\_{k}|^{2}\leq\int\_{G}|\sum\_{k\in\mathbb{Z}}A\_{k}% \mathrm{e}^{\mathrm{i}k(t+x)}|^{2}{\,\rm d}x{\,\rm d}t=\int\_{G}|\sum\_{k\in% \Lambda}a\_{k}\mathrm{e}^{\mathrm{i}k(t+x)}|^{2}{\,\rm d}x{\,\rm d}t. |  |

The observability (A.10) follows similarly.
∎

The following theorem gives a sufficient and necessary condition of the observable set for the transport equation. We only state the result for (A.7), the reader easily figures out the the necessary modifications for (A.8).

###### Theorem A.5.

Let T>0 and G be a measurable subset of [0,T]\times{\mathbb{T}} with positive measure. Then the observability inequality

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\|u\_{0}\|^{2}\_{L^{2}({\mathbb{T}})}\leq C\int\_{G}|u(t,x)|^{2}{\,% \rm d}x{\,\rm d}t |  | (A.11) |

holds with a positive constant C>0 for all solutions to the transport equation (A.7) if and only if there exists a constant c\_{0}>0 such that

|  |  |  |  |
| --- | --- | --- | --- |
|  | \int\_{0}^{T}\mathbf{1}\_{G}(s,x-s){\,\rm d}s\geq c\_{0}\quad\mbox{ for a.e. }x% \in{\mathbb{T}} |  | (A.12) |

###### Proof.

The direction \eqref{equ-tran-suff-necc-2}\Longrightarrow\eqref{equ-tran-suff-necc-1} follows clearly from Proposition A.4. To show the inverse direction, namely \eqref{equ-tran-suff-necc-1}\Longrightarrow\eqref{equ-tran-suff-necc-2}, we use the contradiction argument. Suppose that (A.12) is not true, then for any \varepsilon>0, there exists a set E\_{\varepsilon}\subset{\mathbb{T}} with positive measure |E\_{\varepsilon}|>0 such that

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle\int\_{0}^{T}\mathbf{1}\_{G}(s,x-s){\,\rm d}s<\varepsilon\quad\mbox% { for a.e. }x\in E\_{\varepsilon}. |  | (A.13) |

Let u\_{0\varepsilon}(x)=1\_{E\_{\varepsilon}}(x). Then u\_{0\varepsilon}\in L^{2}({\mathbb{T}}) and its support is contained in E\_{\varepsilon}. Then, similar to the proof in Proposition A.4, we have

|  |  |  |
| --- | --- | --- |
|  | \displaystyle\int\_{G}|u(t,x)|^{2}{\,\rm d}x{\,\rm d}t=\int\_{{\mathbb{T}}}|u\_{0% \varepsilon}(x)|^{2}\int\_{{\mathbb{T}}}\mathbf{1}\_{G}(t,x-t){\,\rm d}t{\,\rm d% }x\leq\varepsilon\|u\_{0\varepsilon}\|^{2}\_{L^{2}({\mathbb{T}})}. |  |

Taking \varepsilon>0 small enough, say \varepsilon=C/2, we obtain a contradiction with (A.11).
∎

### A.3. GCC is necessary for the wave observability

###### Corollary A.6.

If the observability (1.2) holds on some observation set G\subset[0,T]\times{\mathbb{T}}, then G needs to satisfies (GCC).

###### Proof.

The idea is to reduce the proof to that of transport equations.
Assume that

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle u\_{0}(x)=f(x)=\sum\_{k\in\mathbb{Z}}a\_{k}e^{ikx},\quad u\_{1}(x)=g% (x)=\sum\_{k\in\mathbb{Z}}b\_{k}e^{ikx}. |  | (A.14) |

Then the solution to the wave equation (1.1)can be expressed as

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle u(t,x)=a\_{0}+b\_{0}t+\sum\_{k\in\mathbb{Z}\backslash\{0\}}(c\_{k}e^% {ik(x+t)}+d\_{k}e^{ik(x-t)}) |  | (A.15) |

with c\_{k},d\_{k} given by

|  |  |  |  |
| --- | --- | --- | --- |
|  | \displaystyle c\_{k}=\frac{1}{2}(a\_{k}+\frac{b\_{k}}{k}),\quad d\_{k}=\frac{1}{2}% (a\_{k}-\frac{b\_{k}}{k}),\quad k\in\mathbb{Z}\backslash\{0\}. |  | (A.16) |

This implies d’Alembert’s formula on torus

|  |  |  |
| --- | --- | --- |
|  | u(t,x)=\frac{1}{2}(f(x+t)+f(x-t))+\frac{1}{2}\int\_{x-t}^{x+t}g(s){\,\rm d}s. |  |

Assume that the observability inequality holds. Then

|  |  |  |
| --- | --- | --- |