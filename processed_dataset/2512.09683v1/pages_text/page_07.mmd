Note that \mathbbm{E}[1-e^{-\lambda\mathcal{X}}]\geq(1-e^{-\lambda t})\mathbbm{P}[%
\mathcal{X}>t] for t>0. Fix M>0 and take \lambda=\frac{M}{t}, by letting t\to\infty, then we find \varlimsup\limits\_{t\to\infty}\log t\cdot\mathbbm{P}[\mathcal{X}>t]\leq C(1-e^%
{-M})^{-1}. Since M>0 is arbitrary, we have \varlimsup\limits\_{t\to\infty}\log t\cdot\mathbbm{P}[\mathcal{X}>t]\leq C. On the other hand, note that for t>1,

|  |  |  |  |
| --- | --- | --- | --- |
|  | \log t\cdot\mathbbm{E}[1-e^{-\lambda\mathcal{X}}]=\log t\cdot\int\_{0}^{\infty}% \lambda e^{-\lambda s}\mathbbm{P}[\mathcal{X}>s]ds\leq\log t\cdot(1-e^{-% \lambda t})+\log t\cdot e^{-\lambda t}\mathbbm{P}[\mathcal{X}>t] |  | (3.6) |

Choose \alpha>1. Taking \lambda=t^{-\alpha} and then t\to\infty in (3.6) yields \varliminf\limits\_{t\to\infty}\log t\cdot\mathbbm{P}[\mathcal{X}>t]\geq\alpha^%
{-1}C. The lemma then follows by taking \alpha\downarrow 1.
∎

###### Corollary 3.7.

We have \mathbbm{P}[\mathrm{CR}(\rho,0)<\varepsilon]=\frac{3}{\log|\log\varepsilon|}(1%
+o(1)) and \mathbbm{P}[\mathrm{CR}(\widetilde{\ell}\_{1},0)<\varepsilon]=\frac{3}{\log|%
\log\varepsilon|}(1+o(1)) as \varepsilon\downarrow 0.

###### Proof.

By Proposition 3.2, when \lambda\in[0,\frac{1}{12}), we have

|  |  |  |
| --- | --- | --- |
|  | \mathbbm{E}[\mathrm{CR}(\rho,0)^{\lambda}]=\frac{\sin(\frac{\pi}{2}\sqrt{1-12% \lambda})}{\sqrt{1-12\lambda}}-\frac{2\sqrt{3}\sin(\frac{\pi}{3}\sqrt{1-12% \lambda})}{\sqrt{1-12\lambda}\log\left(\frac{(2+\sqrt{3})^{2}-\tan^{2}\left(% \frac{\pi}{12}\sqrt{1-12\lambda}\right)}{1-(2+\sqrt{3})^{2}\tan^{2}\left(\frac% {\pi}{12}\sqrt{1-12\lambda}\right)}\right)}. |  |

In particular, \mathbbm{E}[\mathrm{CR}(\rho,0)^{\lambda}]=1-\frac{3}{|\log\lambda|}(1+o(1)) as \lambda\downarrow 0. By Lemma 3.6 (taking \mathcal{X} there to be -\log\mathrm{CR}(\rho,0)), we find \mathbbm{P}[\mathrm{CR}(\rho,0)<\varepsilon]\sim\frac{3}{\log|\log\varepsilon|} as \varepsilon\downarrow 0.

Similarly, combining Proposition 3.2 and Lemma 3.3, for \lambda\in[0,\frac{1}{12}), we have

|  |  |  |
| --- | --- | --- |
|  | \mathbbm{E}[\mathrm{CR}(\widetilde{\ell}\_{1},0)^{\lambda}]=\mathbbm{E}[\mathrm% {CR}(\rho,0)^{\lambda}]\mathbbm{E}[\mathrm{CR}(\ell\_{1},0)^{\lambda}]=1-\frac{% 2\sqrt{3}\sin(\frac{\pi}{3}\sqrt{1-12\lambda})}{\sin(\frac{\pi}{2}\sqrt{1-12% \lambda})\log\left(\frac{(2+\sqrt{3})^{2}-\tan^{2}\left(\frac{\pi}{12}\sqrt{1-% 12\lambda}\right)}{1-(2+\sqrt{3})^{2}\tan^{2}\left(\frac{\pi}{12}\sqrt{1-12% \lambda}\right)}\right)}. |  |

Hence \mathbbm{E}[\mathrm{CR}(\widetilde{\ell}\_{1},0)^{\lambda}]=1-\frac{3}{|\log%
\lambda|}(1+o(1)) as \lambda\downarrow 0, and we conclude by using Lemma 3.6.
∎

## 4 Proof of Theorem 1.1

In this section, we prove Theorem 1.1 based on the exact laws of the conformal radii in Section 3. Recall that \mathsf{Bac}\_{\varepsilon} is the event that there exist two disjoint subpaths on B[0,\tau\_{\mathbbm{D}}] from \varepsilon\mathbbm{S}^{1} to \frac{1}{2}\mathbbm{S}^{1}.
The lower bound of \mathbbm{P}[\mathsf{Bac}\_{\varepsilon}] in (1.2) then relies on the following continuum version of Menger’s theorem on the connectedness of a graph.

###### Theorem 4.1 ([Nö32, Zip33, Why48]).

Let n\geq 2. Suppose X is a connected, locally connected, and locally compact metric space such that X cannot be disconnected by removing any n-1 points of X. Then for any two points x,y\in X, there exist n arcs connecting x and y, which are pairwise disjoint except on x and y. Here an arc stands for a subset of X that is homeomorphic to [0,1].

###### Lemma 4.2.

\mathsf{Bac}\_{\varepsilon} occurs
if \ell\_{1}\cap\mathbbm{A}\_{1/2}\neq\emptyset and \widetilde{\ell}\_{1}\cap\varepsilon\mathbbm{D}\neq\emptyset.

###### Proof.